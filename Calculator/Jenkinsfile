pipeline {
    agent any 
    stages {
        stage ('pom files'){
        steps{
            sh ' cd Calculator'
        }
    }
        stage('clean') { 
            steps {
                sh 'mvn clean'
            }
        }
        stage('validate') { 
            steps {
                sh 'mvn validate'
            }
        }
    }
}
